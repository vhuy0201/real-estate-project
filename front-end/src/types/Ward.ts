export type Ward = {
    _id: string,
    ward_name: {
        vi: string,
        en: string
    },
    district_id: string,
    deleted: boolean,
    __v: number,
    createdAt: string,
    updatedAt: string
}